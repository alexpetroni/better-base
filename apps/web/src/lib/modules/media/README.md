Media module: S3/MinIO originals, signed imgproxy URLs, media library (Phase 2).

Two barrels: `index.ts` (universal — `<Img>`, validation, types) and `server.ts`
(signing, storage, services — server-only). See docs/STATE.md § Media.
